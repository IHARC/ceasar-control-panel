# Fail2Ban configuration file for ceasar

[Definition]

actionstart = /usr/local/ceasar/bin/v-add-firewall-chain <name>
actionstop = /usr/local/ceasar/bin/v-delete-firewall-chain <name>
actioncheck = iptables -n -L INPUT | grep -q 'fail2ban-<name>[ \t]'
actionban = /usr/local/ceasar/bin/v-add-firewall-ban <ip> <name>
actionunban = /usr/local/ceasar/bin/v-delete-firewall-ban <ip> <name>
