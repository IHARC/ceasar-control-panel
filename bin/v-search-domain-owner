#!/bin/bash
# info: search domain owner
# options: DOMAIN [TYPE]
#
# example: v-search-domain-owner acme.com
#
# This function that allows to find user objects.

#----------------------------------------------------------#
#                Variables & Functions                     #
#----------------------------------------------------------#

# Argument definition
domain=$1
type=${2-any}

# Includes
# shellcheck source=/etc/ceasar/ceasar.conf
source /etc/ceasar/ceasar.conf
# shellcheck source=/usr/local/ceasar/func/main.sh
source $CEASAR/func/main.sh
# load config file
source_conf "$CEASAR/conf/ceasar.conf"

#----------------------------------------------------------#
#                    Verifications                         #
#----------------------------------------------------------#

check_args '1' "$#" 'DOMAIN [TYPE]'

is_format_valid 'domain'

# Perform verification if read-only mode is enabled
check_ceasar_demo_mode

#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#

# Define conf
case $type in
	web) conf="$CEASAR/data/users/*/web.conf" ;;
	dns) conf="$CEASAR/data/users/*/dns.conf" ;;
	mail) conf="$CEASAR/data/users/*/mail.conf" ;;
	*) conf="$CEASAR/data/users/*/*.conf" ;;
esac

owner=$(grep -H "DOMAIN='$domain'" $conf | head -n 1 | cut -f7 -d '/')
if [ -z "$owner" ]; then
	exit "$E_NOTEXIST"
fi

echo "$owner"

#----------------------------------------------------------#
#                       Ceasar                             #
#----------------------------------------------------------#

# Logging
#log_event "$OK" "$ARGUMENTS"

exit
