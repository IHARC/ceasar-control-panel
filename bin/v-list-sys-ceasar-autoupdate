#!/bin/bash
# info: list ceasar autoupdate settings
# options: [FORMAT]
#
# example: v-list-sys-ceasar-autoupdate
#
# This function for obtaining autoupdate settings.

#----------------------------------------------------------#
#                Variables & Functions                     #
#----------------------------------------------------------#

# Argument definition
format=${1-shell}

# Includes
# shellcheck source=/etc/ceasar/ceasar.conf
source /etc/ceasar/ceasar.conf
# shellcheck source=/usr/local/ceasar/func/main.sh
source $CEASAR/func/main.sh
# load config file
source_conf "$CEASAR/conf/ceasar.conf"

# JSON list function
json_list() {
	echo '['
	if [ -z "$check_cron" ]; then
		echo -e "\t\"Disabled\","
	else
		echo -e "\t\"Enabled\""
	fi
	echo "]"
}

# SHELL list function
shell_list() {
	echo -n "AUTOUPDATE:     "
	if [ -z "$check_cron" ]; then
		echo "Disabled"
	else
		echo "Enabled"
	fi
}

# PLAIN list function
plain_list() {
	if [ -z "$check_cron" ]; then
		echo "Disabled"
	else
		echo "Enabled"
	fi
}

# CSV list function
csv_list() {
	echo "AUTOUPDATE"
	if [ -z "$check_cron" ]; then
		echo "Disabled"
	else
		echo "Enabled"
	fi
}

#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#

# Check cron tab
check_cron=$(grep 'v-update-sys-ceasar-all' /var/spool/cron/crontabs/ceasarweb)

# Listing data
case $format in
	json) json_list ;;
	plain) plain_list ;;
	csv) csv_list ;;
	shell) shell_list ;;
esac

#----------------------------------------------------------#
#                       Ceasar                             #
#----------------------------------------------------------#

exit
